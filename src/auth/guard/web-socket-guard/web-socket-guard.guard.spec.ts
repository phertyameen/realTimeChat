import { WebSocketGuardGuard } from './web-socket-guard.guard';

describe('WebSocketGuardGuard', () => {
  it('should be defined', () => {
    expect(new WebSocketGuardGuard()).toBeDefined();
  });
});
